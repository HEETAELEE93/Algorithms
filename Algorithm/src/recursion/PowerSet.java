package recursion;

class Solution{
/*
 powerSet( S ) {	// 멱집합 출력
	if ( S is an empty set ) {
		print nothing;
	} else {
		let t be the first element of s;
		find all subsetx of s-{t} by calling powerSet(s-{t});
		print the subset;
		print the subsets with adding t;
	}
}
 powerSet(P, S) {
 	if ( S is an empty set ) {
 		print P;
 	} else {
 		let t be the first element of S;
 		powerSet(P, s-{t});
 		powerSet(Pu{t}, S-{t});
 	}
 }
 */
	
	
	
}

public class PowerSet {

	public static void main(String[] args) {

	}
}
